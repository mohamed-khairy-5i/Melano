# 🚀 دليل النشر - متجر ميلانو

## 📌 خيارات النشر

### 1️⃣ النشر على Netlify (موصى به)

#### الطريقة الأولى: من خلال GitHub (تلقائي)

1. **التسجيل/تسجيل الدخول**
   - افتح [Netlify](https://app.netlify.com)
   - سجل دخول باستخدام GitHub

2. **استيراد المشروع**
   - اضغط "Add new site"
   - اختر "Import an existing project"
   - اختر "Deploy with GitHub"

3. **اختيار المستودع**
   - ابحث عن `Melano`
   - اختر المستودع

4. **إعدادات البناء** (تُكتشف تلقائياً)
   ```
   Branch: main
   Build command: npm run build
   Publish directory: dist
   ```

5. **النشر**
   - اضغط "Deploy site"
   - انتظر 2-3 دقائق
   - ستحصل على رابط مثل: `https://your-app.netlify.app`

#### الطريقة الثانية: رفع يدوي

1. **بناء المشروع محلياً**
   ```bash
   npm install
   npm run build
   ```

2. **رفع مجلد dist**
   - افتح Netlify
   - اسحب وأفلت مجلد `dist`
   - انتظر اكتمال الرفع

### 2️⃣ النشر على Vercel

```bash
# تثبيت Vercel CLI
npm i -g vercel

# نشر المشروع
vercel
```

### 3️⃣ النشر على GitHub Pages

1. **تحديث vite.config.ts**
   ```typescript
   export default defineConfig({
     base: '/Melano/',
     // ... باقي الإعدادات
   })
   ```

2. **تثبيت gh-pages**
   ```bash
   npm install -D gh-pages
   ```

3. **إضافة سكريبت في package.json**
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```

4. **النشر**
   ```bash
   npm run deploy
   ```

## 🔧 استكشاف الأخطاء

### المشكلة: الصفحة فارغة بعد النشر

**الحل:**
- تأكد من وجود ملف `netlify.toml`
- تأكد من إعداد redirects بشكل صحيح

### المشكلة: خطأ في التوجيه

**الحل:**
- تحقق من `base` في `vite.config.ts`
- تأكد من استخدام `BrowserRouter` وليس `HashRouter`

### المشكلة: الخطوط لا تظهر

**الحل:**
- تحقق من اتصال الإنترنت (Google Fonts)
- تأكد من وجود `@import` في `index.css`

## ⚙️ متغيرات البيئة (إذا لزم الأمر)

قم بإنشاء ملف `.env` في جذر المشروع:

```env
VITE_API_URL=https://your-api.com
VITE_APP_NAME=متجر ميلانو
```

استخدامها في الكود:
```typescript
const apiUrl = import.meta.env.VITE_API_URL;
```

## 📊 مراقبة الأداء

بعد النشر، راقب:
- ✅ سرعة التحميل
- ✅ حجم الملفات
- ✅ وقت الاستجابة
- ✅ أخطاء JavaScript (إن وجدت)

## 🔄 التحديثات التلقائية

عند استخدام Netlify + GitHub:
- كل push إلى فرع `main` يُحدث الموقع تلقائياً
- يمكنك مراجعة التغييرات قبل النشر في Netlify Dashboard

## 📱 اختبار التوافق

اختبر الموقع على:
- ✅ Chrome
- ✅ Firefox
- ✅ Safari
- ✅ Edge
- ✅ الهواتف المحمولة

## 🆘 الدعم

في حال واجهت مشاكل:
1. راجع [Netlify Docs](https://docs.netlify.com/)
2. راجع [Vite Docs](https://vitejs.dev/)
3. تحقق من Console في المتصفح

---

**✨ بالتوفيق في نشر متجر ميلانو!**
