# 🚀 دليل النشر على Netlify | Netlify Deployment Guide

## طريقة النشر السريع | Quick Deployment

### الخطوة 1: إنشاء حساب على Netlify
1. اذهب إلى [Netlify](https://www.netlify.com/)
2. سجل حساب جديد أو سجل الدخول
3. يمكنك التسجيل باستخدام حساب GitHub مباشرة

### الخطوة 2: ربط المستودع
1. من لوحة التحكم في Netlify، اضغط على **"Add new site"**
2. اختر **"Import an existing project"**
3. اختر **GitHub** كمصدر
4. ابحث عن مستودع `Melano` واختره
5. اختر الفرع `main` للنشر

### الخطوة 3: إعدادات البناء
Netlify سيقرأ ملف `netlify.toml` تلقائياً والذي يحتوي على:

```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
```

لا حاجة لتعديل أي شيء! فقط اضغط **"Deploy site"**

### الخطوة 4: انتظر اكتمال البناء
- عملية البناء ستستغرق 2-3 دقائق
- يمكنك مشاهدة سجلات البناء في الوقت الفعلي
- بعد اكتمال البناء، سيعطيك Netlify رابط عام للموقع

### الخطوة 5: تخصيص النطاق (اختياري)
1. من إعدادات الموقع، اختر **"Domain management"**
2. يمكنك تغيير النطاق الفرعي من `random-name.netlify.app` إلى `milano-store.netlify.app`
3. أو ربط نطاق مخصص خاص بك

---

## 🎯 النشر اليدوي | Manual Deployment

إذا أردت بناء المشروع محلياً ورفعه يدوياً:

### الطريقة 1: Netlify CLI

```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# تسجيل الدخول
netlify login

# بناء المشروع
npm run build

# النشر
netlify deploy --prod
```

### الطريقة 2: السحب والإفلات (Drag & Drop)

```bash
# بناء المشروع
npm run build

# بعد ذلك:
# 1. اذهب إلى https://app.netlify.com/drop
# 2. اسحب مجلد dist وأفلته في المتصفح
# 3. انتظر اكتمال الرفع
```

---

## ⚙️ المتغيرات البيئية (إذا لزم الأمر)

إذا كان لديك متغيرات بيئية (API Keys، إلخ):

1. من لوحة تحكم Netlify
2. اذهب إلى **Site settings** → **Build & deploy** → **Environment**
3. أضف المتغيرات المطلوبة

مثال:
```
VITE_API_URL=https://api.example.com
VITE_APP_NAME=Milano Store
```

---

## 🔄 النشر التلقائي | Automatic Deployment

بعد الربط الأولي مع GitHub:

- **كل push إلى فرع `main`** = نشر تلقائي للإنتاج
- **كل Pull Request** = معاينة مؤقتة (Preview Deployment)
- **كل فرع** = يمكن إنشاء معاينة له

---

## 📊 مراقبة الأداء

Netlify يوفر:
- ✅ إحصائيات الزيارات
- ✅ سرعة تحميل الموقع
- ✅ سجلات الأخطاء
- ✅ تحليلات الأداء

---

## 🔒 الأمان

- ✅ HTTPS تلقائي (SSL/TLS مجاني)
- ✅ حماية من DDoS
- ✅ CDN عالمي سريع
- ✅ نسخ احتياطي تلقائي

---

## 💡 نصائح إضافية

### 1. تسريع البناء
أضف هذا إلى `netlify.toml`:
```toml
[build]
  command = "npm run build"
  publish = "dist"
  
[build.processing]
  skip_processing = false
  
[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

[build.processing.images]
  compress = true
```

### 2. إعادة توجيه مخصصة
```toml
[[redirects]]
  from = "/old-page"
  to = "/new-page"
  status = 301
```

### 3. Headers مخصصة
```toml
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
```

---

## 🆘 حل المشاكل | Troubleshooting

### مشكلة: البناء فشل
```bash
# تأكد من أن البناء يعمل محلياً أولاً
npm run build

# إذا نجح محلياً، تحقق من:
# 1. إصدار Node.js في netlify.toml
# 2. جميع dependencies موجودة في package.json
```

### مشكلة: الصفحات لا تعمل (404)
```toml
# تأكد من وجود هذا في netlify.toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### مشكلة: التطبيق لا يتحمل بشكل صحيح
- تحقق من console المتصفح للأخطاء
- تأكد من أن جميع المسارات نسبية
- تحقق من ملف `vite.config.ts`

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- [وثائق Netlify](https://docs.netlify.com/)
- [منتدى Netlify](https://answers.netlify.com/)
- [حالة الخدمة](https://www.netlifystatus.com/)

---

## ✅ قائمة التحقق النهائية

- [ ] تم إنشاء حساب Netlify
- [ ] تم ربط المستودع GitHub
- [ ] تم اختيار فرع main
- [ ] تم التحقق من إعدادات البناء
- [ ] تم بدء عملية النشر
- [ ] تم اختبار الموقع بعد النشر
- [ ] تم تخصيص النطاق (اختياري)
- [ ] تم مشاركة الرابط مع الفريق

---

**🎉 مبروك! تطبيق متجر ميلانو الآن على الإنترنت!**
